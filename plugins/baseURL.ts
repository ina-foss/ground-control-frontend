export default defineNuxtPlugin((nuxtApp) => {
//     let baseURL: string;

//     const nuxtApp = useNuxtApp()
  
//     if (nuxtApp.isClient) {
//       baseURL = 'http://localhost:80';
//     } else if (nuxtApp.isServer) {
//       baseURL = 'http://nginx';
//     } else {
//       baseURL = '';
//     }
  
//     nuxtApp.inject('baseURL', baseURL);
  });